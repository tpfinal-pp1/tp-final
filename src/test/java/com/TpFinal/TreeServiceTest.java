package com.TpFinal;

/**
 * Created by Max on 5/23/2017.
 */
public class TreeServiceTest {
/*
    @Test
    public void agregarTest(){
        TreeService ts = new TreeService();
        Tree tree = new Tree();

        Tag a = new Tag("tag a");
        ts.agregarTag(tree,a);

        Tag b = new Tag("tag b");


        Collection<Object> tags = (Collection<Object>) tree.getItemIds();
        Assert.assertTrue(tags.contains(a));
        Assert.assertFalse(tags.contains(b));
    }

    @Test
    public void existsTest(){
        TreeService ts = new TreeService();
        Tree tree = new Tree();

        Tag a = new Tag("tag a");
        ts.agregarTag(tree,a);

        Tag b = new Tag("tag b");

        Assert.assertTrue(ts.exists(tree,a));
        Assert.assertFalse(ts.exists(tree,b));


    }
    @Test
    public void setearPadreTest(){
        TreeService ts = new TreeService();
        Tree tree = new Tree();

        Tag a = new Tag("tag a");
        ts.agregarTag(tree,a);
        Tag b = new Tag("tag b");
        ts.agregarTag(tree,b);
        Tag c = new Tag("tag c");
        ts.agregarTag(tree,c);
        Tag d = new Tag("tag d");
        ts.agregarTag(tree,d);

        ts.setearPadre(tree,b,a);
        ts.setearPadre(tree,c,a);
        ts.setearPadre(tree,d,b);

        Assert.assertEquals(2,tree.getChildren(a).size());
        Assert.assertEquals(1,tree.getChildren(b).size());



    }
    @Test
    public void quitarTagTreeTest(){
        TreeService ts = new TreeService();
        Tree tree = new Tree();

        Tag a = new Tag("tag a");
        ts.agregarTag(tree,a);
        Tag b = new Tag("tag b");
        ts.agregarTag(tree,b);
        Tag c = new Tag("tag c");
        ts.agregarTag(tree,c);
        Tag d = new Tag("tag d");
        ts.agregarTag(tree,d);

        ts.setearPadre(tree,b,a);
        ts.setearPadre(tree,c,a);
        ts.setearPadre(tree,d,b);

        ts.quitarTagTree(tree,a);
        Assert.assertFalse(ts.exists(tree,a));
        Assert.assertFalse(ts.exists(tree,b));
        Assert.assertFalse(ts.exists(tree,c));
        Assert.assertFalse(ts.exists(tree,d));

    }

*/

}
