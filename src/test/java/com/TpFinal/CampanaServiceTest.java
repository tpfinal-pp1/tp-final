package com.TpFinal;

/**
 * Created by Max on 6/10/2017.
 */
public class CampanaServiceTest {


/*



    @Test
    public void DAOTest() {
        CampanaService campanadeDB = new CampanaService();
        String nombreCampana = "Nombre Campagnola";
        String descripcion = "Descripcion campagnola";
        Date fechaCreacion = new Date();
        int duracion = 2;
        String mensajeTxt = "este es un mensaje de campagnola";
        Mensaje mensaje = null;
        BufferedImage imgImgenMensaje = null;
        if(imgImgenMensaje == null){
            mensaje = new Mensaje(mensajeTxt,null);
        }
        else if(mensajeTxt == null || mensajeTxt.equals("")){
            mensaje = new Mensaje(null,imgImgenMensaje.toString());
        }
        else
            mensaje = new Mensaje(mensajeTxt,imgImgenMensaje.toString());



        Usuario actual = new Usuario("max","pass",new Rol());
        Campana nuevaCampana = new Campana(nombreCampana,descripcion,fechaCreacion,duracion,mensaje,actual);
        campanadeDB.guardarCampana(nuevaCampana);

        campanadeDB.recuperarCampanas(actual);
        Campana camp = campanadeDB.getCampanasGuardadas().get(0);

        Assert.assertTrue(nuevaCampana.equals(camp));

    }
    */

}
